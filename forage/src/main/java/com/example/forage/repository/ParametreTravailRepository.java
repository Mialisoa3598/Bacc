package com.example.forage.repository;

import com.example.forage.model.ParametreTravail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParametreTravailRepository extends JpaRepository<ParametreTravail, Long> {
}